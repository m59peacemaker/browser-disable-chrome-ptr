{
  "name": "disable-chrome-ptr",
  "version": "1.0.0",
  "description": "Disables mobile Chrome's native pull-to-refresh and returns a function to re-enable it.",
  "main": "build/disable-chrome-ptr.cjs.js",
  "module": "src/disable-chrome-ptr.mjs",
  "scripts": {
    "build-main": "rollup -c",
    "build-docs": "(cd docs && npm run build)",
    "build": "npm run build-main && npm run build-docs",
    "clean": "rm -r build docs/build"
  },
  "keywords": [
    "chrome",
    "mobile",
    "native",
    "pull-to-refresh",
    "pull",
    "refresh",
    "disable",
    "prevent",
    "toggle",
    "off"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/m59peacemaker/browser-disable-chrome-ptr"
  },
  "author": "Johnny Hauser <johnnyhauser@gmail.com>",
  "license": "CC0-1.0",
  "devDependencies": {
    "rollup": "^0.45.2",
    "rollup-plugin-commonjs": "^8.1.0",
    "rollup-plugin-node-resolve": "^3.0.0"
  }
}
